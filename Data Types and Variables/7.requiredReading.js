function requariedReading(numberOfPages,pagesRead,days) {
    let totalTime = numberOfPages/pagesRead;
    let requairedHours = totalTime / days;
    console.log(requairedHours)

}
requariedReading(212,20 ,2);